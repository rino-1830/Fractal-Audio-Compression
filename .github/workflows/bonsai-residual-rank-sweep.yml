name: Bonsai residual rank sweep

on:
  push:
    branches:
      - experiment/functional-compression-probe
    paths:
      - "bonsai-cv/residual_rank_sweep.py"
      - ".github/workflows/bonsai-residual-rank-sweep.yml"
  workflow_dispatch:

jobs:
  sweep:
    strategy:
      fail-fast: false
      max-parallel: 4
      matrix:
        include:
          - { layer: 14, fold: 0 }
          - { layer: 14, fold: 1 }
          - { layer: 27, fold: 0 }
          - { layer: 27, fold: 1 }
    runs-on: ubuntu-latest
    timeout-minutes: 50
    env:
      HF_HUB_DISABLE_TELEMETRY: "1"
      TOKENIZERS_PARALLELISM: "false"
      HF_HOME: "/tmp/hf-cache"
      CASE_LAYER: ${{ matrix.layer }}
      CASE_FOLD: ${{ matrix.fold }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"
      - name: Install CPU dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install --index-url https://download.pytorch.org/whl/cpu torch
          python -m pip install "transformers>=4.57,<5" "datasets>=3,<5" "scipy>=1.13,<2" "accelerate>=1,<2" safetensors
      - name: Run residual rank sweep
        working-directory: bonsai-cv
        run: python residual_rank_sweep.py
      - name: Upload result
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: residual-rank-l${{ matrix.layer }}-f${{ matrix.fold }}
          path: bonsai-cv/results-residual-rank-sweep/
          if-no-files-found: warn
