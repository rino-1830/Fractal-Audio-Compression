name: Bonsai deep CV

on:
  push:
    branches:
      - experiment/functional-compression-probe
    paths:
      - "bonsai-cv/deep_cv.py"
      - ".github/workflows/bonsai-deep-cv.yml"
  workflow_dispatch:

jobs:
  deep-cv:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    env:
      HF_HUB_DISABLE_TELEMETRY: "1"
      TOKENIZERS_PARALLELISM: "false"
      HF_HOME: "/tmp/hf-cache"
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"

      - name: Install CPU dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install --index-url https://download.pytorch.org/whl/cpu torch
          python -m pip install "transformers>=4.57,<5" "datasets>=3,<5" "scipy>=1.13,<2" "accelerate>=1,<2" safetensors

      - name: Run deep-layer cross-validation
        working-directory: bonsai-cv
        run: python deep_cv.py

      - name: Publish summary
        if: always()
        run: |
          if [ -f bonsai-cv/results-bonsai-deep-cv/summary.md ]; then
            cat bonsai-cv/results-bonsai-deep-cv/summary.md >> "$GITHUB_STEP_SUMMARY"
          fi

      - name: Upload results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: bonsai-deep-cv
          path: bonsai-cv/results-bonsai-deep-cv/
          if-no-files-found: warn
