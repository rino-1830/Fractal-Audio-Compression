name: FES cross-split robustness

on:
  pull_request:
    paths:
      - "experiments/fes-robust/**"
      - ".github/workflows/fes-robust.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  robustness:
    strategy:
      fail-fast: false
      matrix:
        include:
          - model: EleutherAI/pythia-14m
            slug: pythia14m
          - model: EleutherAI/pythia-31m
            slug: pythia31m
          - model: EleutherAI/pythia-70m
            slug: pythia70m
    runs-on: ubuntu-latest
    timeout-minutes: 60
    env:
      HF_HUB_DISABLE_TELEMETRY: "1"
      TOKENIZERS_PARALLELISM: "false"
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
      - name: Install CPU dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install --index-url https://download.pytorch.org/whl/cpu torch
          python -m pip install transformers datasets scipy
      - name: Decode shared helper
        run: |
          base64 -d experiments/fes-transformer/fes_transformer.py.b64 > experiments/fes-transformer/fes_transformer.py
      - name: Run cross-split robustness
        run: |
          python experiments/fes-robust/fes_robust.py \
            --model "${{ matrix.model }}" \
            --out "results-${{ matrix.slug }}" \
            --cal-windows 4 \
            --test-windows 32
          cat "results-${{ matrix.slug }}/summary.md"
      - name: Upload results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: "fes-robust-${{ matrix.slug }}"
          path: "results-${{ matrix.slug }}/"
          if-no-files-found: warn
