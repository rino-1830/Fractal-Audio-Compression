name: Bonsai graph mask selector

on:
  push:
    branches:
      - experiment/functional-compression-probe
    paths:
      - "bonsai-cv/graph_mask_selector.py"
      - ".github/workflows/bonsai-graph-mask.yml"
  workflow_dispatch:

jobs:
  graph-selector:
    strategy:
      fail-fast: false
      max-parallel: 2
      matrix:
        fold: [0, 1]
    runs-on: ubuntu-latest
    timeout-minutes: 50
    env:
      HF_HUB_DISABLE_TELEMETRY: "1"
      TOKENIZERS_PARALLELISM: "false"
      HF_HOME: "/tmp/hf-cache"
      CASE_FOLD: ${{ matrix.fold }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"
      - name: Install CPU dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install --index-url https://download.pytorch.org/whl/cpu torch
          python -m pip install "transformers>=4.57,<5" "datasets>=3,<5" "accelerate>=1,<2" safetensors
      - name: Run graph mask selector
        working-directory: bonsai-cv
        run: python graph_mask_selector.py
      - name: Upload result
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: graph-mask-f${{ matrix.fold }}
          path: bonsai-cv/results-graph-selector/
          if-no-files-found: warn
