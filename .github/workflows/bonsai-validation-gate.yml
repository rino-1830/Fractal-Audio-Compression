name: Bonsai validation-gated thinning

on:
  push:
    branches:
      - experiment/functional-compression-probe
    paths:
      - "bonsai-cv/onebit_validation_gate.py"
      - ".github/workflows/bonsai-validation-gate.yml"
  workflow_dispatch:

jobs:
  validation-gate:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    env:
      HF_HUB_DISABLE_TELEMETRY: "1"
      TOKENIZERS_PARALLELISM: "false"
      HF_HOME: "/tmp/hf-cache"
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"

      - name: Install CPU dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install --index-url https://download.pytorch.org/whl/cpu torch
          python -m pip install "transformers>=4.57,<5" "datasets>=3,<5" "scipy>=1.13,<2" "accelerate>=1,<2" safetensors

      - name: Run validation-gated thinning
        working-directory: bonsai-cv
        run: python onebit_validation_gate.py

      - name: Publish summary
        if: always()
        run: |
          if [ -f bonsai-cv/results-onebit-validation-gate/summary.md ]; then
            cat bonsai-cv/results-onebit-validation-gate/summary.md >> "$GITHUB_STEP_SUMMARY"
          fi

      - name: Upload results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: bonsai-validation-gate
          path: bonsai-cv/results-onebit-validation-gate/
          if-no-files-found: warn
